#include <string>
#include <vector>
#include <iostream>
#include "../Include/heapsort.h"

using namespace std;


int main(int argc, char **argv){
    //Testing heap...

    /*
    //Initialize input vector
    Heap heap;
    vector<int> input;

    //Populate input vector
    for(int i = 0; i < argc; i++){
        input.push_back(atoi(argv[i]));
    }

    //Erase file name from argv list
    input.erase(input.begin());

    for(unsigned int i = 0; i < input.size()-1; i++){
        heap.insert(input.at(i), input.at(i+1));
    }


    heap.removeMin();
    cout << heap.min().at(0) << "," << heap.min().at(1) << endl;
    cout << heap.heap_size() << endl;
    //cout << heap.min() << endl;
    //heap.removeMin();
    heap.insert(0,4);
    //cout << heap.isEmpty() << endl;

    heap.print();
    */
}
